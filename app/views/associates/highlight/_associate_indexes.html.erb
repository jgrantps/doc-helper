<%# passed to "users/highlight/main_display" via "content_for :main_panel" in view files %>
<%# renders collection of current_user.packages by passing "user/highlight/package" collection partial %>

  <%# byebug %>  

<main class="p-3 inline-flex" id="userr_reference_point">
  <% @associate.accounts.each do |account| %>
   <%a;sldkjf;lkj%>
    <div class="flex-shrink-0 ml-3 w-80 mt-2 p-3 bg-gray-100 rounded">
      <h3 clas="text-sm font-medium text-gray-900"><%= cattegory[:role].titleize.pluralize %></h3>
      <ul class="mt-2">
        <li class="mt-3">                  
          <%= render partial: 'associates/highlight/associate_profiles', locals: {:sorted_associates => current_user.associated_users(category[:role])} %>   
        </li>            
      </ul>
    </div>
  <% end %>  
</main>
