<a href="#" class="flex justify-between item-center px-3 py-1 text-sm font-medium text-gray-700 rounded-lg ">
    <span class="text-sm font-medium text-gray-700"><%= propername(role)+"s"%></span>
    <span class="text-xs font-semibold text-gray-700"><%= current_user.associated_users(role).distinct.count %></span>
</a>
